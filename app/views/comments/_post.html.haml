- if user_signed_in? && log.user != current_user
  = form_with(model: log.comments.build, remote: true) do |f|
    %div= hidden_field_tag :log_id, log.id
    = f.text_area  :comment_text, placeholder: "コメント"
    = button_tag do
      コメント